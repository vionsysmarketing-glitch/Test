{% comment %}
  Accents the last two words of the heading (not just one) so a phrase
  like "Real Flavour" can be highlighted as a unit rather than only its
  final word — falls back to accenting just the last word if the heading
  is shorter than 2 words.
{% endcomment %}
{% assign heading_words = section.settings.heading | split: ' ' %}
{% assign heading_word_count = heading_words.size %}
{% assign heading_accent_words = 2 %}
{% if heading_word_count < 2 %}
  {% assign heading_accent_words = 1 %}
{% endif %}
{% assign heading_lead_count = heading_word_count | minus: heading_accent_words %}
{% assign heading_lead = heading_words | slice: 0, heading_lead_count | join: ' ' %}
{% assign heading_last = heading_words | slice: heading_lead_count, heading_accent_words | join: ' ' %}

<section class="hero" style="--hero-bg: {{ section.settings.background_color }}; --hero-stage-color: {{ section.settings.stage_color }}; --hero-stage-color-alt: {{ section.settings.stage_color_alt }};">
  <div class="hero__deco hero__deco--1" style="color: #FFFFFF; --deco-float-duration: 5.2s; --deco-float-delay: -2.6s;">{% render 'deco-chili' %}</div>
  <div class="hero__deco hero__deco--2" style="color: #FFE8CF; --deco-float-duration: 6.5s; --deco-float-delay: -0.9s;">{% render 'deco-leaf-branch' %}</div>
  <div class="hero__deco hero__deco--3" style="color: #FFFFFF; --deco-float-duration: 4.8s; --deco-float-delay: -3.9s;">{% render 'deco-tamarind' %}</div>

  {% comment %}
    A flat vertical line where the two panel colors meet reads as a plain
    CSS accident rather than a design choice. This mirrors the wave
    dividers already used between sections elsewhere in the theme, just
    oriented vertically — two complementary fills sharing one curve (so
    they tile with no gap/overlap) instead of a straight seam. Desktop
    only: the two panels stack vertically on mobile, so there's no
    vertical seam to soften there.
  {% endcomment %}
  <svg class="hero__seam" viewBox="0 0 120 560" preserveAspectRatio="none" aria-hidden="true">
    <path d="M0,0 L60,0 C90,35 90,105 60,140 C30,175 30,245 60,280 C90,315 90,385 60,420 C30,455 30,525 60,560 L0,560 Z" style="fill: var(--hero-bg);"></path>
    <path d="M120,0 L60,0 C90,35 90,105 60,140 C30,175 30,245 60,280 C90,315 90,385 60,420 C30,455 30,525 60,560 L120,560 Z" style="fill: var(--hero-stage-color);"></path>
  </svg>

  <div class="hero__half hero__half--left">
    <div class="hero__left-ring" aria-hidden="true"></div>
    <div class="hero__watermark" aria-hidden="true">{% render 'deco-swirl' %}</div>
    <div class="hero__deco hero__deco--7" style="color: #FFFFFF; --deco-float-duration: 5.5s; --deco-float-delay: -1.2s;">{% render 'deco-swirl' %}</div>
    <div class="hero__deco hero__deco--8" style="color: #FFE8CF; --deco-float-duration: 6.2s; --deco-float-delay: -3.4s;">{% render 'deco-chili' %}</div>
    <div class="hero__deco hero__deco--9" style="color: #FFFFFF; --deco-float-duration: 4.6s; --deco-float-delay: -0.6s;">{% render 'deco-mortar-pestle' %}</div>
    <div class="hero__deco hero__deco--10" style="color: #FFE8CF; --deco-float-duration: 5.8s; --deco-float-delay: -2.1s;">{% render 'deco-garlic' %}</div>

    <div class="hero__leaf-badge">
      <span aria-hidden="true">🌶️</span> {{ section.settings.eyebrow }}
    </div>
    <h1 class="hero__title hero__title--compact">{{ heading_lead }} <span class="hero__title-accent">{{ heading_last }}</span></h1>

    {% comment %} Hardcoded — see scroll-showcase.liquid for why. {% endcomment %}
    <div class="hero__features">
      <div class="hero__feature">
        <span class="hero__feature-icon" style="--feature-color: #4A6B1F;">{% render 'icon-leaf' %}</span>
        <span class="hero__feature-label">Natural Ingredients</span>
      </div>
      <div class="hero__feature">
        <span class="hero__feature-icon" style="--feature-color: #014C9D;">{% render 'icon-shield' %}</span>
        <span class="hero__feature-label">Zero Preservatives</span>
      </div>
      <div class="hero__feature">
        <span class="hero__feature-icon" style="--feature-color: #9D0101;">{% render 'icon-flame' %}</span>
        <span class="hero__feature-label">Small-Batch Made</span>
      </div>
      <div class="hero__feature">
        <span class="hero__feature-icon" style="--feature-color: #B5651D;">{% render 'icon-truck' %}</span>
        <span class="hero__feature-label">Free Shipping</span>
      </div>
    </div>

    <div class="hero__actions">
      <a href="{{ section.settings.primary_link }}" class="btn btn--solid">{{ section.settings.primary_label }}</a>
      <a href="{{ section.settings.secondary_link }}" class="btn btn--outline">{{ section.settings.secondary_label }}</a>
    </div>
  </div>

  <div class="hero__half hero__half--right">
    <div class="hero__deco hero__deco--4" style="color: #9D0101;">{% render 'deco-swirl' %}</div>
    <div class="hero__deco hero__deco--6" style="color: #4A6B1F;">{% render 'deco-leaf-branch' %}</div>

    {% comment %}
      Same clean, no-card/no-circle/no-dark-panel product feature as
      before, but now auto-advancing through the three spice-powder
      products instead of showing just one — an automatic slider for the
      Masala range. JS toggles "is-active" on the matching slide/caption/
      dot; markup falls back to showing just the first product if JS
      never runs (progressive enhancement, same pattern as the "Meet the
      Pack" scroll showcase).
    {% endcomment %}
    {% comment %}
      Link fallbacks are resolved by looking up the real product by title
      (collections.all always has the merchant's actual URL/handle) rather
      than guessing a handle like '/products/chilli-flakes' — real store
      handles often carry extra suffixes (e.g. "chilli-flakes-export-quality")
      that a guessed default can't predict, and a wrong guess 404s instead
      of failing safely. Falls through to the shop-all link, never a dead end.
    {% endcomment %}
    {% assign slide_titles = 'Garam Masala Powder|Chilli Flakes|Dhaniya Powder' | split: '|' %}
    {% assign slide_tags = '★ Best Seller|🔥 Customer Favorite|🌿 Everyday Staple' | split: '|' %}
    {% assign slide_images = 'garam-masala.jpg|chilli-flakes.jpg|coriander-powder.jpg' | split: '|' %}
    {% assign masala_product = collections.all.products | where: 'title', 'Garam Masala (Export Quality)' | first %}
    {% assign chilli_product = collections.all.products | where: 'title', 'Chilli Flakes (Export Quality)' | first %}
    {% assign coriander_product = collections.all.products | where: 'title', 'Coriander powder (Export Quality)' | first %}
    {% assign safe_fallback = section.settings.shop_all_link | default: '/collections/all' %}
    {% assign slide_links = section.settings.masala_link | default: masala_product.url | default: safe_fallback %}
    {% assign slide_links_2 = section.settings.chilli_link | default: chilli_product.url | default: safe_fallback %}
    {% assign slide_links_3 = section.settings.coriander_link | default: coriander_product.url | default: safe_fallback %}

    <div class="hero__feature-slider" data-hero-slider>
      <span class="hero__feature-product-eyebrow">{{ section.settings.category_label }}</span>

      <div class="hero__feature-slide-viewport">
        <div class="hero__feature-slide is-active" data-slide="0">
          <span class="hero__feature-product-shadow" aria-hidden="true"></span>
          <img class="hero__feature-product-image" src="{{ slide_images[0] | asset_url }}" alt="{{ slide_titles[0] }}" loading="eager" width="320" height="256">
        </div>
        <div class="hero__feature-slide" data-slide="1">
          <span class="hero__feature-product-shadow" aria-hidden="true"></span>
          <img class="hero__feature-product-image" src="{{ slide_images[1] | asset_url }}" alt="{{ slide_titles[1] }}" loading="lazy" width="320" height="256">
        </div>
        <div class="hero__feature-slide" data-slide="2">
          <span class="hero__feature-product-shadow" aria-hidden="true"></span>
          <img class="hero__feature-product-image" src="{{ slide_images[2] | asset_url }}" alt="{{ slide_titles[2] }}" loading="lazy" width="320" height="256">
        </div>
      </div>

      <a href="{{ slide_links }}" class="hero__feature-caption is-active" data-caption="0">
        <span class="hero__feature-product-name">{{ slide_titles[0] }}</span>
        <span class="hero__feature-product-tag">{{ slide_tags[0] }}</span>
      </a>
      <a href="{{ slide_links_2 }}" class="hero__feature-caption" data-caption="1">
        <span class="hero__feature-product-name">{{ slide_titles[1] }}</span>
        <span class="hero__feature-product-tag">{{ slide_tags[1] }}</span>
      </a>
      <a href="{{ slide_links_3 }}" class="hero__feature-caption" data-caption="2">
        <span class="hero__feature-product-name">{{ slide_titles[2] }}</span>
        <span class="hero__feature-product-tag">{{ slide_tags[2] }}</span>
      </a>

      <div class="hero__feature-dots" role="tablist" aria-label="Featured spice powders">
        <button type="button" class="hero__feature-dot is-active" data-dot="0" aria-label="Show {{ slide_titles[0] }}"></button>
        <button type="button" class="hero__feature-dot" data-dot="1" aria-label="Show {{ slide_titles[1] }}"></button>
        <button type="button" class="hero__feature-dot" data-dot="2" aria-label="Show {{ slide_titles[2] }}"></button>
      </div>
    </div>

    <a href="{{ section.settings.shop_all_link | default: '/collections/all' }}" class="hero__range-pill">
      {{ section.settings.range_label }} <span aria-hidden="true">→</span>
    </a>
  </div>

  <svg class="hero__wave" viewBox="0 0 1440 60" preserveAspectRatio="none" aria-hidden="true">
    <path d="M0,0 C240,50 480,10 720,32 C960,54 1200,15 1440,40 L1440,60 L0,60 Z" fill="{{ section.settings.wave_color | default: '#FBF3E7' }}"></path>
  </svg>
</section>

{% schema %}
{
  "name": "Hero",
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "Small-batch. Big flavour." },
    { "type": "text", "id": "heading", "label": "Heading", "default": "Nothing Beats Real Flavour" },
    { "type": "text", "id": "primary_label", "label": "Primary button label", "default": "Shop the Pack" },
    { "type": "url", "id": "primary_link", "label": "Primary button link" },
    { "type": "text", "id": "secondary_label", "label": "Secondary button label", "default": "Our Story" },
    { "type": "url", "id": "secondary_link", "label": "Secondary button link" },
    { "type": "text", "id": "range_label", "label": "CTA card text", "default": "Shop the Full Range" },
    { "type": "color", "id": "background_color", "label": "Left panel color", "default": "#E88B23", "info": "Bold flat color behind the headline" },
    { "type": "color", "id": "stage_color", "label": "Right panel color", "default": "#FFFFFF", "info": "Matches the product photo's own white background so it floats seamlessly with no visible card edge" },
    { "type": "color", "id": "stage_color_alt", "label": "Product label accent color", "default": "#9D0101", "info": "Used for the category/best-seller text labels" },
    { "type": "color", "id": "wave_color", "label": "Bottom wave color", "default": "#FBF3E7", "info": "Should match the page background so the section blends in on scroll" },
    { "type": "url", "id": "shop_all_link", "label": "\"Shop the Full Range\" link" },
    { "type": "header", "content": "Featured product slider" },
    { "type": "text", "id": "category_label", "label": "Category label", "default": "Spice Powders" },
    { "type": "url", "id": "masala_link", "label": "Garam Masala Powder link", "info": "Defaults to /products/garam-masala-powder" },
    { "type": "url", "id": "chilli_link", "label": "Chilli Flakes link", "info": "Defaults to /products/chilli-flakes" },
    { "type": "url", "id": "coriander_link", "label": "Dhaniya Powder link", "info": "Defaults to /products/coriander-powder" }
  ]
}
{% endschema %}
